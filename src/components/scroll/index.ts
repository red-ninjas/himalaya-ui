export interface InnerScrollEvent {
  scrollTop: number;
  scrollHeight: number;
  clientHeight: number;
}

export { default as InnerScroll } from './inner-scroll';
export type { InnerScrollProps } from './inner-scroll';
