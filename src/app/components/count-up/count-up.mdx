import { Playground, Attributes } from 'lib/components'
import { CountUp, Text, Button } from 'components'
import React, { useState } from 'react'

export const meta = {
  title: 'Count Up',
  group: 'Data Display',
}

# CountUp

`CountUp` is used to show animation counter

<Playground
  title="CountUp example"
  desc="Example of Countup"
  scope={{ CountUp, Text, Button }}
  code={`
<CountUp start={0} end={4000} easing="linear" isCounting={true}>
  {({ value }) => (
    <Text m={0} font={'72px'} style={{ fontWeight: '500' }}>
      {value}+
    </Text>
  )}
</CountUp>
`}
/>

<Attributes component="CountUp" edit="/app/components/count-up.mdx"></Attributes>
