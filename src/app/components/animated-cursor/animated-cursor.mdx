import { Playground, Attributes } from 'lib/components'
import { AnimatedCursor } from 'components'

export const meta = {
  title: 'Animated Cursor',
  group: 'Others',
}

# Animated Cursor

An `AnimatedCursor` UI component to customizable cursor animations for interactivity.

<Playground
  scope={{ AnimatedCursor }}
  title={'General'}
  desc={'An animated cursor component'}
  code={`
    <AnimatedCursor> </AnimatedCursor>
  `}
/>

<Attributes component="AnimatedCursor" edit="/app/components/animated-cursor.mdx"></Attributes>
