import { Playground, Attributes } from 'lib/components';
import { Menu, Link } from 'components';
import MoreHorizontal from 'components/icons/moreHorizontal';
import { Home } from 'components/icons';

export const meta = {
  title: 'Menu',
  group: 'Data Display',
};

# Menu

Easily create and customize menus for your applications with the `Menu` UI component.

<Playground
  scope={{ Menu, Link, Home }}
  desc={'Example of a menu'}
  code={`
  () => {
     return (
    <Menu content="Menu">
			<Menu.Item  icon={ <Home  size={18} />}>
				<Link href="/">Link Item 1</Link>
			</Menu.Item>
			<Menu.Item icon={ <Home   size={18} />}>
				<Link href="/">Link Item 1</Link>
			</Menu.Item >
			<Menu.Item>
				<Link href="/">Link Item 1</Link>
			</Menu.Item>
			<Menu.Item>
				<Link href="/">Link Item 1</Link>
			</Menu.Item>
		</Menu>
    );
  };
  `}
/>

<Playground
  scope={{ Menu, Link, MoreHorizontal }}
  title={'Menu with custom trigger icon'}
  code={`
  () => {
     return (
    <Menu content={<MoreHorizontal />}>
			<Menu.Item>
				<Link href="/">Link Item 1</Link>
			</Menu.Item>
		</Menu>
    );
  };
  `}
/>

<Attributes component="Menu" edit="/app/components/menu.mdx"></Attributes>
