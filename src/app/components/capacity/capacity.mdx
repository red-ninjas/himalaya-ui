import { Layout, Playground, Attributes } from 'lib/components';
import { Capacity, Spacer} from 'components';

export const meta = {
  title: 'Capacity',
  group: 'Data Display',
};

# Capacity

Display an capacity indicator.

<Playground
  scope={{ Capacity, Spacer }}
  code={`
<>
  <Capacity value={15} /> <Spacer />
  <Capacity value={45} /> <Spacer />
  <Capacity value={95} />
</>
`}
/>

<Playground
  title="Fixed Color"
  desc="Specify a color to override the default."
  scope={{ Capacity, Spacer}}
  code={`
() => {
  return <Capacity value={75} color='var(--color-primary-1000)' />
}
`}
/>

<Attributes component="Capacity" edit="/app/components/capacity.mdx"></Attributes>
