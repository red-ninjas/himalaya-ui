import { Layout, Playground, Attributes } from 'lib/components'
import { GradientContent } from 'components'

export const meta = {
  title: 'Gradient Content',
  group: 'Layout',
}

# Gradient content

The `GradientContent` UI component enables gradient backgrounds and lazy-loaded images to enhance your web application's visual appeal.


<Playground
  scope={{ GradientContent }}
  title={'Gradient Content example'}
  desc={'Example of a gradient Content'}
  code={`
  <GradientContent height={'300px'} gradientheight={'50%'}>
    Test
  </GradientContent>
  `}
/>

<Playground
  scope={{ GradientContent }}
  title={'Gradient Content example with image'}
  desc={'Example of a gradient image'}
  code={`
  <GradientContent height={'300px'} gradientheight={'50%'}>
    Test
  </GradientContent>
  `}
/>

<Attributes edit="/app/components/gradient-content.mdx">
<Attributes.Title>GradientContent.Props</Attributes.Title>

| Attribute      | Description                                             | Type             | Accepted values          | Default                                             |
| -------------- | ------------------------------------------------------- | ---------------- | ------------------------ | --------------------------------------------------- |
| gradient       | Gradient in css format for background style             | `string`         | -                        | `linear-gradient(to bottom, accents_1, background)` |
| img            | path to a image which will be lazy loaded as background | `string`         | -                        | -                                                   |
| gradientheight | the height of the gradient to the relative element      | `string`         | -                        | `50vh`                                              |
| ...            | native props                                            | `HTMLAttributes` | `'id', 'className', ...` | -                                                   |

</Attributes>
