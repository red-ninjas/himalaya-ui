import { Playground, Attributes } from 'lib/components';
import { Header } from 'components';

export const meta = {
  title: 'Header',
  group: 'Layout',
};

# Header

The `Header` UI component provides a customizable header section for your web application, with options for creating a visually appealing and functional top bar.

<Playground
  scope={{ Header }}
  title={'Header example'}
  desc={'Example of a header'}
  code={`<Header transcluent={false}>
  <Header.Left>
    Logo
  </Header.Left>
  <Header.Center>
    Middle part
  </Header.Center>
  <Header.Right><span>Right</span></Header.Right>
  <Header.Right><span>Part</span></Header.Right>
  </Header>
  `}
/>

<Attributes component="Header" edit="/app/components/header.mdx"></Attributes>
