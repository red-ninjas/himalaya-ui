import { Playground, Attributes } from 'lib/components'
import { Spinner } from 'components'

export const meta = {
  title: 'Spinner',
  group: 'Feedback',
}

# Spinner

Indicate an action running in the background.

As an indicator of load waiting, see also component [Loading](/components/loading).

<Playground
  scope={{ Spinner }}
  code={`
<Spinner />
`}
/>

<Attributes component="Spinner" edit="/app/components/spinner.mdx"></Attributes>

