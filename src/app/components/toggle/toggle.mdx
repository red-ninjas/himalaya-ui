import { Playground, Attributes } from 'lib/components';
import { Toggle, Spacer } from 'components';

export const meta = {
  title: 'Toggle',
  group: 'Data Entry',
};

# Toggle

Displays a boolean value.

<Playground
  scope={{ Toggle, Spacer }}
  code={`
<>
  <Toggle />
  <Spacer h={.5} />
  <Toggle initialChecked />
</>
`}
/>

<Playground
  title="Type"
  desc="Differentiate states by color."
  scope={{ Toggle, Spacer }}
  code={`
<>
  <Toggle type="primary" initialChecked />
  <Spacer h={.5} />
  <Toggle type="default" initialChecked />
  <Spacer h={.5} />
  <Toggle type="secondary" initialChecked />
  <Spacer h={.5} />
  <Toggle type="success" initialChecked />
  <Spacer h={.5} />
  <Toggle type="warning" initialChecked />
  <Spacer h={.5} />
  <Toggle type="error" initialChecked />
</>
`}
/>

<Playground
  title="Disabled"
  scope={{ Toggle, Spacer }}
  code={`
<>
  <Toggle disabled />
  <Spacer h={.5} />
  <Toggle initialChecked disabled />
</>
`}
/>

<Playground
  title="Change event"
  scope={{ Toggle, Spacer }}
  code={`
() => {
  const handler = event => {
    console.log(event.target.checked)
  }
  return <Toggle onChange={handler} />
}
`}
/>

<Attributes component="Toggle" edit="/app/components/toggle.mdx"></Attributes>
