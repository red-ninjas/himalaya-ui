import { QuickBar, QuickAction, Divider, Box } from 'components';
import { Playground, Attributes } from 'lib/components';
import { Anchor, Code, Github, Home, Layout } from 'components/icons';
import NextLink from 'next/link';

export const meta = {
  title: 'Quick Bar',
  group: 'Navigation',
};

# Quick Bar

QuickBar is a customizable sidebar component designed for navigation .

<Playground
  title="Example"
  scope={{
    QuickAction,
    Code,
    Home,
    Anchor,
    Github,
    Layout,
    QuickBar,
    Divider,
    Box,
  }}
  style={{ position: 'relative' }}
  desc={'A fully example of using the QuickBar'}
  code={`
<Box w={60} h={12} style={{ position: 'relative' }}>
      <QuickBar>
          <QuickAction active r={50}  tooltip='Home'><Home size={20} /></QuickAction>
          <Divider w={'100%'}></Divider>
          <QuickAction r={50} tooltip='Guide'><Code size={20} /></QuickAction>
          <Divider w={'100%'}></Divider>
          <QuickAction r={50}  tooltip='Components'><Layout size={20} /></QuickAction>
      </QuickBar>
</Box>
  `}
/>

<Playground
  title="Example NextJS"
  scope={{
    QuickAction,
    Code,
    Home,
    Anchor,
    Github,
    Box,
    Layout,
    QuickBar,
    Divider,
    NextLink,
  }}
  style={{ position: 'relative' }}
  desc={'A fully example of using the QuickBar'}
  code={`
<Box w={60} h={12} style={{ position: 'relative' }}>
    <QuickBar>
        <NextLink href={'#'} passHref legacyBehavior>
          <QuickAction active r={50} tooltip='Home'><Home size={20} /></QuickAction>
        </NextLink>
    </QuickBar>
</Box>
  `}
/>

<Attributes edit="/app/components/quick-bar.mdx"></Attributes>
