import { Layout, Attributes } from 'lib/components'

export const meta = {
  title: 'Toast',
  group: 'Feedback',
}

# Toast

The Toast component only supports Hooks calls, please see the [useToast](/hooks/use-toast 'Toast Hooks') documentation for details.

<Attributes edit="/app/components/toast.mdx" />
