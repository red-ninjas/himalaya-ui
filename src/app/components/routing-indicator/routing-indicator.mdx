import { Playground, Attributes } from 'lib/components'
import { RoutingIndicator } from 'components'

export const meta = {
  title: 'Routing Indicator',
  group: 'Navigation',
}

# Routing Indicator

The `RoutingIndicator` UI component provides visual feedback during page transitions, enhancing the user experience by showing the progress of navigation. 

<Playground
  scope={{ RoutingIndicator }}
  title={'General'}
  desc={'A routing indicator component to show progress during navigation'}
  code={`
    <RoutingIndicator>
      {/* Your content */}
    </RoutingIndicator>
  `}
/>

<Attributes component="RoutingIndicator" edit="/app/components/routing-indicator.mdx"></Attributes>
